[tool.poetry]
name = "startings"
version = "0.1.0"
description = "Register python scripts as bootstrapped"
authors = ["jawide <596929059@qq.com>"]
readme = "README.md"
packages = [{include = "startings"}]

[tool.poetry.dependencies]
python = ">=3.7"
daemonprocessing = "^0.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
