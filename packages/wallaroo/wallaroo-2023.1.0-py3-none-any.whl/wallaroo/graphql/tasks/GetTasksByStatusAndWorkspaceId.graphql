query GetTasksByStatusAndWorkSpaceId(
    $status: task_status_enum!,
    $workspace_id: bigint!
) {
    task(where: {status: {_eq: $status}, workspace_id: {_eq: $workspace_id}}) {
        id
        flavor
        subject
        attempt
    }
}
