[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "brugernavn"
version = "0.1.0b1"
authors = [
  { name="Jonathan Krüger", email="jonathan.krueger@teckids.org" },
]
description = "A OSINT tool to search for a username on many websites"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]
install_requires=["typer", "requests"]

[project.urls]
"Homepage" = "https://edugit.org/pinguin/brugernavn"
"Repository" = "https://edugit.org/pinguin/brugernavn"
