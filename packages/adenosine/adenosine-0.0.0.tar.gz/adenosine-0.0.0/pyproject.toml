[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta:__legacy__"

[tool.isort]
profile = "black"
line_length = 96

[project]
name = "adenosine"
version = "0.0.0"
authors = [
  { name="bryan newbold", email="bnewbold@robocracy.org" },
]
description = "Basic, enthusiast-grade python client for atproto.com and bsky.app"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://gitlab.com/bnewbold/adenosine"
"Bug Tracker" = "https://gitlab.com/bnewbold/adenosine/-/issues"
