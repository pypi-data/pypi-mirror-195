build:
    root_path: './'
    environment:
        python:
            version: 3.9.9
            virtualenv: true
    dependencies:
        before:
            -
                command: export PATH="$HOME/.cargo/envbin:$PATH" ; curl https://sh.rustup.rs -sSf | sh -s -- -y ; source "$HOME/.cargo/env"

