[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stac-info"
version = "0.0.1"
description = "A Python package for STAC info"
dependencies = [
    'requests',
]