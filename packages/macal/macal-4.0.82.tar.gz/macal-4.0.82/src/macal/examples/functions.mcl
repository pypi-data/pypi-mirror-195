// This is the root scope.
include system;
var = 13;
display => (arg) {
    // This is the function scope. It cannot use the variable "var" 
    // unless it is defined here. If you uncomment the next line
    // the language will report: "Runtime error: Variable not found var @(8, 13)"
    // Console(var);
    Console(arg);
}

func = display;

func(42);
