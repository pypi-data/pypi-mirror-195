[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120

[tool.isort]
line_length = 120
multi_line_output = 3
default_section = "THIRDPARTY"
known_first_party = "vectice"
known_local_folder = "tests"
include_trailing_comma = true
