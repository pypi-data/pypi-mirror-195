#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['sqlalchemy_fdw']

package_data = \
{'': ['*']}

install_requires = \
['sqlalchemy>=1.3', 'psycopg2-binary']

entry_points = \
{'sqlalchemy.dialects': ['pgfdw = sqlalchemy_fdw.dialect:dialect']}

setup(name='sqlalchemy_fdw',
      version='0.4.1',
      description='PostgreSQL dialect supporting foreign data wrappers',
      author='Kozea',
      author_email=None,
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
