[tool.poetry]
name = "trr-ds-core"
version = "0.0.0a"
description = "dummy trr-ds-core"
authors = ["Scyther"]
readme = "README.md"
packages = [{include = "trr_ds_core"}]

[tool.poetry.dependencies]
python = "~3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
