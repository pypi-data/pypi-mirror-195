[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sphinxext-bsb"
description = "BSB Sphinx documentation extension"
version = "0.2.0"
dependencies = [
    "sphinx-design"
]
