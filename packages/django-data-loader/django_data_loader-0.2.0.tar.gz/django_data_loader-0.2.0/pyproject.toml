[tool.poetry]
name = "django-data-loader"
version = "0.2.0"
description = "Data fetching layer for Django class-based views to provide batching and caching."
authors = ["Josiah Kaviani <proofit404@gmail.com>"]
license = "BSD-2-Clause"
readme = "README.md"
packages = [{include = "django_data_loader"}]

[tool.poetry.dependencies]
python = "*"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
