[tool.poetry]
name = "colabmount"
version = "1.1.0"
description = "Easily mount a file from Google Drive in Google Colab"
authors = ["Kevin Katz <contact@kevinkatz.io>"]

# [tool.poetry.dependencies]
# google-colab = "^1.0.0"

[build-system]
requires = ["poetry-core>=1.0.0", "poetry-dynamic-versioning"]
build-backend = "poetry_dynamic_versioning.backend"
